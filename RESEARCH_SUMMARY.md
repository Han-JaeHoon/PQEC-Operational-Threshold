# 연구 진행 내용 종합 정리 (Research Summary)

> PQEC Operational Threshold — 저장소 전체(`main` + 단계별 브랜치 + `iterated-noisy-pqec`)와
> 2026년 8월 계산 노트 5편을 통합 정리한 문서.
>
> 대상 노트: `00_Main_Results_and_Research_Implications`, `01_Step3_Analytic_Repeated_Dynamics`,
> `02_Step4_Analytic_Repeated_Dynamics`, `03_Step5_Full_State_Repeated_Dynamics`,
> `04_Verification_and_Circuit_Level_Mechanism` (Jaehun Han, August 2026).

---

## 0. 연구의 위치와 질문

출발점 논문 — J. Raghoonanan & T. Byrnes, *Quantum Error Correction by Purification*,
arXiv:2603.11568 (2026) — 은 **데이터에만 노이즈를 걸고 정제(purification) 단계는 완벽**하다고
가정한 뒤 PQEC를 분석한다. 실제 하드웨어에서는 정제 게이지 자체 — 특히 SWAP test 심장부의
3-큐빗 controlled-SWAP(Fredkin) — 가 노이즈를 가진다.

**이 연구의 질문**: *PQEC 연산 자체의 노이즈*가 얼마나 커질 때까지 정제가 여전히 얽힘을
회복시키는가? 즉 **operational threshold**를 찾는다.

연구는 두 축으로 전개되었다.

| 축 | 질문 | 결과물 |
|--|--|--|
| **A. 한 라운드 임계값** (Setup → Step 1–5) | 한 번의 노이즈 PQEC 라운드가 손익분기(`F_out > F_bare`)를 넘는 per-CNOT 노이즈 상한 `ε₂*`는? | 저장소 `main`, 단계별 브랜치 |
| **B. 반복 동역학** (Aug 2026 노트 5편) | 같은 노이즈 라운드를 **반복**하면 상태는 어디로 가는가? 고정점, 안정성, 장시간 극한은? | `iterated-noisy-pqec` / `claude/theoretical-update-review-*` 브랜치 + 노트 5편 |

**핵심 발견 한 줄 요약**: 같은 이상 유니터리 `U_PQEC`를 구현하는 서로 다른 회로 분해들은
CNOT 개수와 무노이즈 정확도가 같아도 **노이즈 하에서 질적으로 다르게 행동한다**. 한 라운드
성능 순위가 장시간 반복 거동의 순위를 결정하지 않는다.

---

## 1. 공통 규약

### 1.1 상태와 표기

```
|Φ⁺⟩ = (|00⟩ + |11⟩)/√2 ,     Φ⁺ = |Φ⁺⟩⟨Φ⁺| = ¼(II + XX − YY + ZZ)

입력 (등방성/Bell-isotropic):
ρ_ε = (1−ε) Φ⁺ + ε·II/4 = ¼[ II + ε̄ (XX − YY + ZZ) ],   ε̄ := 1 − ε
```

- `F_bare(ε̄) = (1 + 3ε̄)/4 = 1 − 3ε/4`
- Bell 스펙트럼: `Φ⁺ → 1 − 3ε/4`, 나머지 세 Bell 상태 → `ε/4`
- 순도 `Tr(ρ_ε²) = (1−3ε/4)² + 3(ε/4)²`
- **얽힘 조건**: `F > 1/2` ⟺ `ε < 2/3`
- 문서에 따라 `t = 1 − ε` 와 `ε̄ = 1 − ε` 가 혼용되지만 **같은 양**이다.

### 1.2 회로와 읽기

```
와이어 순서 (a, A₁, A₂, B₁, B₂) = (0,1,2,3,4)
  a = SWAP-test 보조 큐빗, A = 유지 레지스터, B = 버리는 두 번째 사본

σ_in = |0⟩⟨0|_a ⊗ ρ_A ⊗ ρ_B ,   ρ_A = ρ_B = ρ
U_PQEC = H_a · CSWAP_{a;A₂B₂} · CSWAP_{a;A₁B₁} · H_a
```

두 가지 등가 읽기 (둘 다 기계 정밀도로 검증됨):

- **상태 추출** (논문 Eq. 9): `ρ² = (보조 |0⟩ 블록) − (|1⟩ 블록)` ⟹ `P(ρ) = ρ²/Tr(ρ²)`
- **관측량 / 패리티 상관자** (논문의 실제 프로토콜):
  `⟨O⟩_purified = ⟨Z_a ⊗ O⟩ / ⟨Z_a ⊗ I⟩ = Tr(Oρ²)/Tr(ρ²)`

### 1.3 노이즈 모형 (Step 3 이후 전 구간 공통)

**전제: CNOT만 노이즈를 가지며 단일 큐빗 게이트는 이상적이다.**
모든 CNOT 직후에 2-큐빗 **치환형(replacement) 감극 채널**을 삽입한다.

```
D_q^(ij)(A) = (1−q) A + q [ I_ij/4 ⊗ Tr_ij(A) ] ,     q̄ := 1 − q
```

(`q`는 문서에 따라 `ε₂`로도 표기됨 — 동일한 per-CNOT 노이즈 강도.)

Heisenberg 그림에서 이 채널의 수반은 Pauli 문자열에 대해 매우 단순하다:
`D_q^(ij)†(P) = P` (P_i = P_j = I 일 때), 그 외에는 `q̄P`. 이 성질이 Step 3/4의 닫힌 형태
유도 전체를 가능하게 한다.

---

## 2. 축 A — 한 라운드 operational threshold

### Setup — 노이즈 입력 상태 `ρ_ε` (완료)

`noisy_bell_state.py`. `H·CNOT`로 `|Φ⁺⟩`를 만든 뒤, 16개 2-큐빗 Pauli Kraus 연산자를 가진
**하나의 `QubitChannel`** 로 전역 2-큐빗 감극 채널(강도 `ε`)을 적용 — 진짜 혼합 상태 회로.

**검증**: 해석적 목표와의 일치 `max|ρ − ρ_ε| ≈ 2.8e-16` (임의 `ε ∈ [0,1]`, 500회 랜덤),
단위 대각합·에르미트성·양반정치·Bell 스펙트럼·충실도·순도 전부 통과.

### Step 1 — 이상적 1라운드 PQEC 기준선 (완료)

`pqec_gadget.py`. 5-와이어 SWAP-test 게이지. `purify_once` / `purify_rounds` / `obs_purified`.

- 상태 추출: 랜덤 상태 500개에서 `purify_once(ρ) = ρ²/Tr(ρ²)` — `~1e-16`
- 관측량 프로토콜: 랜덤 200개에서 `⟨Z⊗O⟩/⟨Z⊗I⟩ = Tr(Oρ²)/Tr(ρ²)` — `~1e-16`

**결과**: `ρ_ε`에서 `|Φ⁺⟩`는 모든 `ε < 1`에 대해 엄격히 지배적인 고유벡터
(`1−3ε/4 > ε/4`)이므로 이상 게이지는 **모든 `ε < 1`에서** `F, C → 1`. 심지어
**분리 가능한 입력(`2/3 ≤ ε < 1`)도 재얽힘**시킨다. `ε = 1`의 `I/4`만이 고정점.

닫힌 형태: `F₁ = (1+3t)² / (4(1+3t²))`, `t = 1 − ε`.

### Step 2 — CSWAP 수준 **전역** 감극 노이즈 (완료 — 임계값 없음)

각 Fredkin 직후 그것이 건드린 3 큐빗(보조 포함)에 3-큐빗 전역 감극 `g_F`:
`G(σ) = (1−g_F)σ + g_F (I₈/8)⊗Tr_S(σ)`.

**결과: 이 모형은 자기-완화(self-mitigating)된다 — 신호 손실만 있고 임계값이 없다.**
두 패리티 상관자가 **똑같이** `(1−g_F)²` 배로 줄어들어 비율에서 상쇄되므로

```
F_PQEC(p, g_F) = (1+3α²)² / (4(1+3α⁴)) = F_ideal(p),   α = 1 − 4p/3,  α² = 1 − ε
```

즉 `0 ≤ g_F < 1` 전 구간에서 정제 충실도가 `g_F`와 **무관**하다. 유일한 비용은 샘플링
오버헤드 발산 `N_samp ∼ (1−g_F)^{-4}`. 해석식 전부를 회로로 `~1e-13`까지, 손계산 중간 상태
`σ₀…σ_out` 전부를 `~1e-16`까지 재현(`verify_note_states.py`).

**교훈**: 진짜 operational threshold를 얻으려면 분자와 분모를 **비대칭적으로** 감쇠시키는
노이즈가 필요하다 → 분해된 Fredkin의 CNOT 노이즈(Step 3).

### Step 3 — 교과서 **16-CNOT** 분해 + per-CNOT 노이즈 (완료)

`CSWAP(q;a,b) = CNOT(b→a)·Toffoli(q,a;b)·CNOT(b→a)`, Clifford+T Toffoli(6 CNOT)
⟹ **Fredkin 1개 = 8 CNOT**, **라운드당 16 CNOT**.

**결과: 유한한 operational threshold `ε₂*`가 나타난다.** 닫힌 형태 (`v = 1−ε₂`, `t = 1−ε`;
회로와 `~1e-14` 일치):

```
F_dec = ¼[ 1 + t(1+t)(v⁵ + 5v⁶) / (1 + 3v⁴t²) ]
임계값: (1+t)(v⁵+5v⁶) = 3(1 + 3v⁴t²) 의 근
ε₂ → 0 기울기:  K₂ = t(1+t)(33t²+35) / (4(1+3t²)²)  → 17/8  (깨끗한 입력)
```

| 입력 `ε` | 0.10 | 0.20 | 0.30 | 0.40 | 0.50 | 0.60 |
|--|--|--|--|--|--|--|
| `ε₂*` | 0.033 | 0.061 | 0.085 | 0.103 | 0.117 | 0.126 |

임계값은 **입력 노이즈가 클수록 커진다**(얻을 게 많으니 더 나쁜 CNOT도 견딘다). 또한 노이즈
CNOT은 등방성 입력을 **비등방성** Bell-대각 유효 상태(`c_z > c_⊥`)로 만든다. 현실적인
하드웨어 CNOT 오류(`~10⁻²`)보다 `ε ≳ 0.03`에서 이미 충분히 위에 있다.

### Step 4 — 재합성 **14-CNOT** (정확 유니터리 보존) (완료)

`H·CSWAP·CSWAP·H`는 고정된 5-큐빗 유니터리다. Qiskit / pytket 두 peephole 최적화기가
**16 → 14 CNOT**으로 줄이며, 각각 **동일한 유니터리**임을 확인 (`14 = 2×7`, Fredkin당 최적).
PennyLane에서 동일하게 재구현(유니터리 겹침 `1.000000000000`, `ε₂=0`에서 읽기값이
`Tr(Oρ²)/Tr(ρ²)`와 `1e-16` 일치).

| 입력 `ε` | 0.10 | 0.20 | 0.30 | 0.40 | 0.50 | 0.60 |
|--|--|--|--|--|--|--|
| Step 3 (16) | 0.033 | 0.061 | 0.085 | 0.103 | 0.117 | 0.126 |
| **Step 4 (14)** | **0.041** | **0.079** | **0.112** | **0.140** | **0.162** | **0.178** |

**1.25–1.42× 향상** — 단순 개수비 `16/14 ≈ 1.14`보다 **크다**. 즉 기계가 찾은 배치는 개수
절감 이상으로 노이즈 전파도 유리하다.

### Step 5 — 학습·가지치기된 **14-CNOT** (완료)

**보조 배경 — 일반 ansatz는 표현력/훈련가능성 압착에 걸린다.** 정확한 해석적 그라디언트,
다수 재시작, barren-plateau 완화용 **LHST** 국소 비용, 375 파라미터 / 24 CNOT까지 써도
최선의 infidelity가 `δ ≈ 0.44`에서 바닥. 교사–학생 테스트(`test_inclass.py`)는 이것이
**그 일반 ansatz들에 특유한** 압착(얕으면 훈련되나 표현 못하고, 깊으면 표현하나 훈련 안 됨)
이지 게이지가 컴파일 불가능하다는 뜻이 **아님**을 보인다.

**게이지-정합 ansatz가 `U`를 정확히 컴파일한다** (`pqc_ring_ansatz.py`). 세 가지를 고정하면 된다:

1. **게이지-정합 연결성** `(0,1)(0,3)(1,3)(0,2)(0,4)(2,4)` — 보조 큐빗↔두 레지스터 + swap 쌍
   (선형 체인 `0-1-2-3-4`는 20 CNOT에서도 `U`를 표현 못 함 — 표현력 문제)
2. **모든 CNOT 뒤에 완전한 RX,RY,RZ 층** (블록 사이만이 아니라)
3. 충분한 깊이: `L=1`(6 CNOT) `δ=0.86` → `L=2`(12) `0.44` → **`L=3`(18) `δ≈3e-15` 정확**

**14로 가지치기** (`pqc_ring_prune.py`): 18-CNOT 정확해에서 warm-start greedy 제거 →
**14 CNOT, 여전히 정확**. **13은 도달 불가** — 어떤 단일 제거도 `δ ≥ 0.15`를 남김(`reach13.py`).
즉 learn-then-prune과 peephole이 **독립적으로 14로 수렴**.

**배치가 임계값을 정한다 — 개수가 아니라** (`pqc_ring_threshold.py`):

| 입력 `ε` | 교과서 (16) | Step 4 (14) | **학습 (14)** | 파괴적 (2) |
|--|:--:|:--:|:--:|:--:|
| 0.10 | 0.033 | 0.041 | **0.060** | 0.146 |
| 0.40 | 0.103 | 0.140 | **0.237** | 0.313 |
| 0.60 | 0.126 | 0.178 | **0.338** | 0.343 |

같은 `U`의 정확한 14-CNOT 구현 두 개가 **1.5–1.9×** 차이 난다. 견고성은 **창발적** —
회로는 노이즈 없이 훈련·가지치기되었다.

### 보조 비교들 (주 흐름 밖, 참조용)

**파괴적 / VD 2-CNOT 게이지** (`destructive_gadget.py`). PQEC는 SWAP *유니터리*가 필요한 게
아니라 숫자 하나 `F = Tr(Oρ²)/Tr(ρ²)` 가 필요하다 — 그것은 두 사본에 대한 **SWAP 연산자의
기대값**이다. controlled-SWAP을 *적용*하는 대신 SWAP을 직접 *측정*: 쌍마다 Bell 기저 변환
(`CNOT + H` 한 세트) 후 읽기. 보조 큐빗 없음, controlled-SWAP 없음 — **2 CNOT**.

```
F_dest(t, ε₂) = (1 + 6s²t + 9s²t²) / (4(1 + 3s²t²)),   ε₂* = 1 − 1/√(2 + 2t − 3t²),  s = 1−ε₂
```

controlled-SWAP 게이지와 **정확히 같은 `F`** (충실도 사영 및 일반 Pauli 모두 `2e-16`),
임계값 **~2.7–4.4× 높음**. 단, (i) 고정 `ε₂`에서의 *평균 충실도* 임계값이고, (ii) 일반
분자는 여러 Pauli 측정 **세팅**이 필요하므로 "2 CNOT"은 *세팅당* 비용이며, (iii) 결맞은
정제 상태가 아니라 `⟨O⟩`만 돌려주는 **측정 전용** 방식이다.

**CNOT 절감의 출처 — 사다리** (같은 ansatz + greedy prune, `O ∈ {Φ⁺, ZZ}`):

| 회로가 재현하는 대상 | 최소 CNOT |
|--|:--:|
| 완전 유니터리 `U` | **14** (13 불가) |
| 보조-`\|0⟩` isometry `U₀` | **14** (유니터리와 동일) |
| 관측량 `F`, 보조-패리티 읽기 | **5** |
| 관측량 `F`, 구조화된 파괴적 읽기 | **2** |

**교훈**: 절감은 전적으로 **관측량 완화**에서 온다. 유니터리 → 결맞은 상태(isometry) 완화는
아무것도 벌어주지 못한다(14 → 14).

**노이즈 인식 훈련 — 음성 결과.** 관측량 완화(`B=6` CNOT)로 학습한 `θ_free`는 `F_exact(ε)`를
`~0.001`로 맞추고, 배치하면 16-CNOT 교과서를 모든 입력 노이즈에서 이긴다
(`ε₂*` = 0.051 → 0.273). 그러나 CNOT 노이즈를 손실 함수에 **넣는 것은 임계값 이득이 없다**:
비율 `F`를 맞추면 분모 축퇴가 생기고, 절대 상관자를 맞추면 비율과 어긋난다. 노이즈가
**유니탈**이라 유니터리 재매개화로 역전시킬 수 없다는 점과 정합적이지만, **경험적 음성 결과이지
불가능성 증명은 아니다.** 또한 `θ_free`는 *입력족*(unseen `ε`, 오차 0.0014/0.0021)에는
일반화하지만 *관측량*에는 일반화하지 않는다(unseen `XX, YY`에서 `~0.13` 오차) — 즉
Bell-등방성 입력과 `{Φ⁺, ZZ}`에 특화된 **학습된 추정기**이지 일반 관측량-등가 게이지가 아니다.

---

## 3. 축 B — 반복 노이즈 PQEC 동역학 (Aug 2026 노트 5편)

### 3.1 반복 맵의 정의

```
σ_in = |0⟩⟨0|_a ⊗ ρ_n ⊗ ρ_n
τ^(k)(ρ) = Tr_{a,B}[ (Z_a ⊗ I_AB) E_q^(k)(σ_in) ]        (패리티 가중 유지-레지스터 연산자)
Q_k(ρ) = Tr[τ^(k)(ρ)]
M_q^(k)(ρ) = τ^(k)(ρ) / Q_k(ρ) ,        ρ_{n+1} = M_q^(k)(ρ_n)
```

각 라운드가 `ρ ⊗ ρ`를 소비하므로 맵은 `ρ`에 대해 **이차(bilinear)** 이고, 정규화가
**비선형성**을 만든다.

> ⚠️ **범위 주의**: `τ_A`는 패리티-*가중* 연산자이지 라운드의 무조건부 물리적 출력이 아니다.
> 이를 반복한다는 것은 재구성된 유효 상태를 다음 라운드의 두 동일 입력으로 다시 준비할 수 있다고
> 가정하는 것 — 즉 **유효 비선형 맵**이지 샷 단위 실험 프로토콜이 아니다.

세 회로 모두 같은 이상 `U_PQEC`를 목표로 하되 분해가 다르다:
Step 3 = 교과서 16 CNOT, Step 4 = 컴파일러 재합성 14 CNOT, Step 5 = 학습된 14 CNOT.

### 3.2 한 라운드 약노이즈 기울기 (순수 Bell 입력)

```
F₁^(3)(1,q) = 1 − (17/8) q + O(q²)      (= 2.125 q)
F₁^(4)(1,q) = 1 −  (7/4) q + O(q²)      (= 1.75  q)
F₁^(5)(1,q) = 1 −  (5/4) q + O(q²)      (= 1.25  q)
```

**Step 5가 한 라운드 손실이 가장 작다.** (이 순서는 축 A의 `ε₂*` 순서와 일치한다.)

Step 5의 한 라운드 닫힌 형태 (회로 의존 계수 `α = 0.0189647281751`):

```
Q₅(ε̄,q̄) = (q̄¹²/4)(1 + 3ε̄²)
N_Φ,5(ε̄,q̄) = (q̄¹²/16)[ 1 + {3q̄(1+q̄) + α(1−q̄)²} ε̄ + (3 + q̄ + 5q̄²) ε̄² ]
K₅(ε̄) = ε̄(9 + 11ε̄) / (4(1 + 3ε̄²))
```

### 3.3 Step 3 — Bell-대각 섹터의 **정확한** 반복 맵 (노트 01)

Heisenberg 역전파로 최종 관측량을 얻는다:

```
M_I^(f) ≐ ¼[ q̄¹⁰ ZIIII + q̄¹⁴(ZXXXX + ZYYYY + ZZZZZ) ]
M_X^(f) ≐ (q̄¹⁶/4)(ZIIXX + ZXXII − ZYYZZ − ZZZYY)
M_Y^(f) ≐ (q̄¹⁶/4)(ZIIYY − ZXXZZ + ZYYII − ZZZXX)
M_Z^(f) ≐ ¼( q̄¹⁵ZIIZZ − q̄¹⁶ZXXYY − q̄¹⁵ZYYXX + q̄¹⁶ZZZII )
```

일반 Bell-대각 입력 `ρ(x,y,z) = ¼[II + xXX + yYY + zZZ]`에 대해:

```
x′ = 2q̄⁶(x − yz) / D ,   y′ = 2q̄⁶(y − xz) / D ,   z′ = q̄⁵(1+q̄)(z − xy) / D
D = 1 + q̄⁴(x² + y² + z²)
```

⟹ **`M_q^(3) : S_BD → S_BD`, Bell-대각 섹터가 정확히 불변.**

Bell-등방성 초기값 `(x₀,y₀,z₀) = (ε̄, −ε̄, ε̄)`은 `q>0`에서 한 매개변수 직선을 벗어나지만
(`XX/YY`와 `ZZ`가 다르게 감쇠), 평면 `y = −x`는 정확히 불변이다. `u := x = −y`, `v := z`로 두면

```
u_{n+1} = 2q̄⁶ u_n (1 + v_n) / [1 + q̄⁴(2u_n² + v_n²)]
v_{n+1} = q̄⁵(1+q̄)(v_n + u_n²) / [1 + q̄⁴(2u_n² + v_n²)]
F(u,v) = (1 + 2u + v)/4
```

**고정점** (`q → 0`에서 목표 Bell 상태로 가는 가지):

```
A(q̄) = q̄³ + q̄² − q̄ + 1
Δ(q̄) = q̄⁴A(q̄)² + (5q̄+1)(1+q̄)(2q̄⁶ − 1)
v_*^(3) = [q̄²A(q̄) + √Δ(q̄)] / [q̄²(5q̄+1)]
u_*^(3) = √( v_*(q̄ − 1 + 2q̄v_*) / (1 + q̄) )
```

약노이즈 전개:

```
u_*^(3)(q) = 1 − 3q − (81/16)q² + O(q³)
v_*^(3)(q) = 1 − (5/2)q − (85/16)q² + O(q³)
F_*^(3)(q) = 1 − (17/8)q − (247/64)q² + O(q³)
```

**존재 한계 (saddle-node)**: `Δ(q̄_SN) = 0` ⟹ `q_SN^(3) ≈ 0.1305793`.
**얽힘 한계**: `C_* = max{0, 2F_* − 1}` ⟹ `F_* = 1/2` 에서 `q_ent^(3) ≈ 0.1291375`.
따라서 `0.129137 ≲ q ≲ 0.130579` 의 좁은 구간에서는 고정점이 **존재하지만 이미 분리 가능**하다.

대표값:

| q | `u_*` | `v_*` | `F_*` |
|--|--|--|--|
| 0.01 | 0.969470 | 0.974443 | 0.978346 |
| 0.05 | 0.833597 | 0.857754 | 0.881237 |
| 0.10 | 0.602256 | 0.647161 | 0.712918 |
| 0.12 | 0.450744 | 0.501193 | 0.600670 |

정합성: `q = 0`에서 맵이 정확히 `ρ ↦ ρ²/Tr(ρ²)`로 환원되고, Bell-등방성 입력에 대해 이전에
독립적으로 유도한 한 라운드 Step 3 결과를 재현한다.

### 3.4 Step 4 — 같은 구조, 더 얕은 지수 (노트 02)

동일한 유도가 Step 4에서도 성립한다. 차이는 `q̄`의 지수뿐이다 (16 CNOT → 14 CNOT):

```
Q₄ = (q̄¹⁰/4)[1 + q̄²(x²+y²+z²)]        (Step 3은 q̄⁴)
x′ = 2q̄⁴(x − yz)/D₄ ,  y′ = 2q̄⁴(y − xz)/D₄ ,  z′ = q̄³(1+q̄)(z − xy)/D₄
D₄ = 1 + q̄²(x² + y² + z²)

u_{n+1} = 2q̄⁴ u_n(1 + v_n) / [1 + q̄²(2u_n² + v_n²)]
v_{n+1} = q̄³(1+q̄)(v_n + u_n²) / [1 + q̄²(2u_n² + v_n²)]
```

⟹ **Step 4도 Bell-대각 섹터를 정확히 보존한다.**

```
Δ₄(q̄) = q̄²A(q̄)² + (5q̄+1)(1+q̄)(2q̄⁴ − 1)
       = q̄⁸ + 2q̄⁷ + 9q̄⁶ + 12q̄⁵ + 5q̄⁴ − 2q̄³ − 4q̄² − 6q̄ − 1
v_*^(4) = [q̄A(q̄) + √Δ₄(q̄)] / [q̄(5q̄+1)]

v_*^(4)(1−q) = 1 − 2q − 2q² − (57/8)q³ + O(q⁴)
u_*^(4)(1−q) = 1 − (5/2)q − (15/8)q² − (107/16)q³ + O(q⁴)
F_*^(4)(q)   = 1 − (7/4)q − (23/16)q² − (41/8)q³ + O(q⁴)
```

**존재 한계**: `q̄_SN ≈ 0.8105833` ⟹ `q_SN^(4) ≈ 0.1894167`
(saddle-node에서 `u_SN ≈ 0.19530`, `v_SN ≈ 0.27292`, `F_SN ≈ 0.41588`).
**얽힘 한계**: `2u_* + v_* = 1` ⟹ `112q̄⁶ − 16q̄⁵ − 19q̄⁴ + 10q̄³ − 52q̄² + 14q̄ − 1 = 0`,
관련 근 `q̄_ent ≈ 0.8152642` ⟹ `q_ent^(4) ≈ 0.1847358`.
여기서도 `0.184736 ≲ q ≲ 0.189417`은 존재하지만 분리 가능한 좁은 구간.

| q | `F_*^(3)` | `F_*^(4)` |
|--|--|--|
| 0.001 | 0.997871 | 0.998249 |
| 0.010 | 0.978346 | 0.982351 |
| 0.050 | 0.881237 | 0.908138 |

**Step 4 재합성은 점근 고정점 충실도까지 개선한다.** 1차 손실 `7/4 < 17/8`.
(위 닫힌 형태·전개·임계값은 본 정리 작성 시 독립적으로 수치 재확인함 — 아래 §6.)

**섹터 내 안정성**: 축소된 2차원 Jacobian의 고유값 크기는 약노이즈에서
`|λ₁| = 2q + O(q²)`, `|λ₂| = (5/4)q + O(q²)` — 섹터 안에서는 강하게 끌어당긴다.

### 3.5 Step 5 — Bell-대각 대칭성 깨짐과 전체 상태 동역학 (노트 03)

**회로**. 19개의 완전한 단일 큐빗 회전층(`L_ℓ = ⊗_j RZ(γ)RY(β)RX(α)`)과 18개 CNOT 슬롯 중
**14개 활성 CNOT**: `V₅ = L₁₈ G₁₈ L₁₇ ⋯ G₂ L₁ G₁ L₀`, 총 `19×5×3 = 285` 파라미터.
시간 순 CNOT 목록:

```
(0,1),(1,3),(0,4),(2,4),(0,3),(1,3),(0,4),(2,4),(0,1),(0,3),(1,3),(0,2),(0,4),(2,4)
```

전역 위상 정렬 후 `max|(V₅^align − U_PQEC)_ij| ≈ 2.34e-8`, `‖V₅^align − U_PQEC‖_F ≈ 2.04e-7`.

**보정(calibration)**. 학습 잔차를 노이즈 효과와 분리하기 위해 근사-항등 보정
`C_corr = U_PQEC (V₅^align)†` 을 후합성한다. 보정 모형 `M_q,cal^(5)`은 `q = 0`에서 임의의
2-큐빗 `ρ`에 대해 정확히 `ρ²/Tr(ρ²)`를 준다. **CNOT 위치와 그 노이즈 채널은 그대로 유지된다.**

**대칭성 깨짐 (핵심)**. 정확한 Bell-대각 입력 `Φ⁺`에 대해 한 라운드 후 1차 off-Bell 성분:

```
Π_⊥[ M_q,cal^(5)(Φ⁺) − Φ⁺ ]
  = (q/4)[ −0.08277891 XY + 0.08277891 YX
           + 0.01628305 XZ + 0.01628305 ZX
           − 0.00167576 YZ + 0.00167576 ZY ] + O(q²)
```

⟹ **`M_q^(5)(S_BD) ⊄ S_BD`.** 보정을 제거해도 동일한 지배적 1차 구조가 나오므로
**보정의 인공물이 아니다.** 따라서 Step 3/4의 `(u,v)` 2변수 기술은 Step 5에 **불충분**하고,
15개 Pauli 좌표 전체를 추적해야 한다:

```
r′_λ = Σ_{μν} B_{λ;μν}(q̄) r_μ r_ν  /  Σ_{μν} B_{0;μν}(q̄) r_μ r_ν      (유리 이차 맵)
B_{λ;μν}(q̄) = (1/16) Tr[ (Z_a ⊗ P_λ^(A) ⊗ I_B) E_q^(5)( |0⟩⟨0|_a ⊗ P_μ^(A) ⊗ P_ν^(B) ) ]
```

Bell 섹터 1차 계수는 `⟨XX⟩′ = 1 − 1.55775941q`, `⟨YY⟩′ = −1 + 1.62011360q`,
`⟨ZZ⟩′ = 1 − 1.82212699q` 이고, 합치면 `F′ = 1 − (5/4)q` — 한 라운드 결과를 독립적으로 재현.

**고충실도 준안정 대지 → 탈출** (`ε̄ = 0.9`, `q = 0.01`):

```
F₁ ≈ 0.985465  F₂ ≈ 0.987490  F₁₀ ≈ 0.987507
F₁₀₀ ≈ 0.9875070   F₅₀₀ ≈ 0.9873997
F₁₀₀₀ ≈ 0.5442     F₂₀₀₀ ≈ 0.4086     F₅₀₀₀ ≈ 0.40797
```

즉 **초기 정제 → 고충실도 준안정 대지 → 탈출**. 전체 상태 Jacobian이
`max|λ_i(J_full)| ≈ 1.01156 > 1` (q = 0.01) 로 이를 뒷받침한다. 노이즈가 약할수록 불안정성이
약해 대지가 수천 라운드 지속될 수 있다.

**장시간 극한 — 곱 상태 고정 원(product fixed circle)**. 탈출 후 동역학은

```
ρ_prod = ω(r₁) ⊗ ω(r₂) ,     ω(r) = ½(I + r·σ)
```

로 **수치적으로 불변**인 곱 상태족에 접근한다. 두 번째 큐빗의 국소 맵은
`g_q̄(r) = M₂(q̄) r / (1 + |r|²)`, `M₂(q̄) = (1+q̄)I₃ − (1−q̄) n̂₂n̂₂^T`,
`n̂₂ ≈ (−0.393418339, −0.519666236, 0.758398981)`. `n̂₂`에 수직인 방향에서
`r′ = (1+q̄)r/(1+r²)` 이므로 `r_* = √q̄ = √(1−q)` — **`n̂₂`에 수직인 모든 단위 벡터가 고정점**,
즉 **원**을 이룬다. 첫 번째 큐빗은 축 `n̂₁ ≈ (0.111159524, 0.888111251, 0.445984267)`의 음의
방향에서 같은 반지름을 갖는다.

```
ρ_prod,*^(5)(q, θ) = ω(−√(1−q) n̂₁) ⊗ ω(√(1−q) m̂(θ)) ,   m̂(θ) ⊥ n̂₂
```

- 스펙트럼: `{ (1+√(1−q))²/4 , q/4 , q/4 , (1−√(1−q))²/4 }`
- 순도: `Tr(ρ_*²) = (1 − q/2)²` → 약노이즈에서 **1에 접근**
- 그러나 곱 상태이므로 **동시성(concurrence) `C = 0`, 음성도(negativity) `N = 0`**
- Bell 충실도는 `q`만으로 정해지지 않고 원 위 각도에 의존:
  `F_Φ,*^(5) = ¼[1 + q̄(−n₁ₓmₓ + n₁ᵧmᵧ − n₁_z m_z)]`
- 안정성: 반지름 방향은 `h′(r_*) = q/(2−q) ∈ (0,1)` 로 **끌어당김**, 각 방향은 `λ = 1` 로 **주변부(marginal)**

`q = 0.01` 예: `r₁,* ≈ (−0.11060, −0.88366, −0.44375)`, `r₂,* ≈ (−0.75457, 0.64650, 0.05156)`,
`F_Φ,* ≈ 0.40797`, 스펙트럼 `{0.99499, 0.00250, 0.00250, 0.0000063}`, 순도 `0.990025`, `C = 0`.

**즉 장시간 Step 5 상태는 매우 순수하지만 얽힘이 전혀 없다.**

### 3.6 검증과 회로 수준 메커니즘 (노트 04)

노트 04는 네 가지 독립 검증을 수행한다.

**검증 1 — 전체 상태 off-Bell 누출.** 32×32 5-큐빗 밀도 행렬을 완전히 전파하고 유지
레지스터의 15개 Pauli 계수를 모두 독립 추출. `q = 0.01`에서 최대 off-Bell 진폭:

| 회로 | `max_{P∈S⊥} \|r_P\|` |
|--|--|
| Step 3 | 9.1e-17 |
| Step 4 | 8.9e-16 |
| Step 5 (보정) | **8.20e-4** |

1차 계수: Step 3은 `a_XX = −3, a_YY = +3, a_ZZ = −5/2`, Step 4는 `−5/2, +5/2, −2`,
둘 다 off-Bell 성분은 수치 정밀도 내에서 0. `F_Φ = ¼(1 + r_XX − r_YY + r_ZZ)`로 각각
`1 − 17/8 q`, `1 − 7/4 q`, `1 − 5/4 q`를 재현 — 축 A와 완전히 정합.

**검증 2 — 전체 상태 안정성.** 이중선형성을 이용해 **유한 차분 없이 정확한** Jacobian을 만든다:

```
δτ_j = B_q(P_j/4, ρ_*) + B_q(ρ_*, P_j/4)
J_ij = Tr[ P_i ( δτ_j/Q_* − τ_*·δQ_j/Q_*² ) ]
```

| | Step 3 (q=0.01) | Step 4 (q=0.01) | Step 3 (q=0.05) | Step 4 (q=0.05) |
|--|--|--|--|--|
| 2D (섹터 내) | 0.0310111 | — | — | — |
| **전체 15D** | **1.023065303478** | **1.017818374848** | **1.128500701479** | **1.096119112573** |

⟹ **Step 3/4의 고정점은 Bell-대각 섹터 안에서는 끌개지만, 전체 2-큐빗 상태 공간에서는
안장점(saddle)이다.** 주요 불안정 방향은 두 경우 모두 대략 `(ZI + IZ)/√2`.

**검증 3 — 씨앗을 넣은 비선형 탈출.** `ρ₀ = ρ_* + (η/4)√2 (ZI + IZ)`로 섭동:

- 1스텝 증폭비가 Jacobian 고유값과 수치 정밀도까지 일치
  (Step 3: 1.023065303451 vs 1.023065303478; Step 4: 1.017818374852 vs 1.017818374848)
- 탈출 시간이 `n_esc ∝ ln(1/|η|)` 로그 스케일링을 따름:

| 회로 | η = 1e-8 | 1e-6 | 1e-4 |
|--|--|--|--|
| Step 3 (예측/관측) | 605.855 / 606 | 403.903 / 404 | 201.952 / 202 |
| Step 4 (예측/관측) | 782.239 / 783 | 521.493 / 522 | 260.746 / 261 |

- **장시간 종점**: `ρ_Z,±^(k) = ¼[II ± m_k(ZI + IZ) + c_k ZZ]` — `C = N = 0` 인 **분리 가능한
  안정 끌개** (전체 Jacobian 고유값 < 1). 정확한 곱 상태는 아님 (`c_k ≠ m_k²`).
- **임의의 off-Bell 섭동이 탈출을 일으키지는 않는다**: Step 3의 안정 off-Bell 모드
  `|λ_stable| = 0.012943659413` 은 `1e-4 → 1.294e-6 → 1.675e-8 → 2.169e-10` 로 감쇠한다.
  섭동이 **불안정 고유방향과 겹쳐야** 탈출한다.
- 정확한 산술에서 `ρ₀ ∈ S_BD ⟹ ρ_n ∈ S_BD` 이므로 Step 3/4의 Bell-등방성 궤적은 불안정
  방향을 **여기시키지 않는다**. 유한 정밀도 반올림이 만드는 인공 씨앗은 매 라운드 `S_BD`로
  사영해 제거하면 수천 회 반복에도 `~1e-16` 로 유지된다.

**검증 4 — 1차 CNOT 결함 분해.** 단일 CNOT 위치 `j`의 치환 채널을 `D_q,j = I + q(R_j − I)`로 쓰면

```
E_q^(k) = U^(k) + q Σ_j L_j^(k) + O(q²) ,     L_j^(k) = U_{>j}^(k) ∘ (R_j − I) ∘ U_{≤j}^(k)
T_j^(k) = ‖ off-Bell 1차 응답 벡터 ‖₂
```

- Step 3: `max_j T_j = 5.59e-17`, 총합 `1.47e-16`
- Step 4: `max_j T_j = 1.25e-15`, 총합 `3.08e-15`

⟹ **Step 3/4의 무누출은 큰 개별 응답들의 상쇄가 아니다** — 개별 결함 각각이 이미 off-Bell
응답을 만들지 않는다.

- Step 5는 `T_j = O(10⁻¹)` 인 위치가 여럿이고 지배적인 것은 **`j = 2, 4, 6, 8`**
  (각각 `A₁→B₁`, `A₂→B₂`, `A₁→B₁`, `A₂→B₂`). 이 넷의 합이 만드는 6성분 벡터의 노름이
  `0.119333938` 인데, 14개 CNOT 전체의 off-Bell 도함수 벡터 노름은 `0.119334101` —
  차이 `6.31e-7` (상대 `5.29e-6`). **네 위치가 사실상 전체 누출을 설명한다.**
- **지지(support)만으로는 설명되지 않는다**: 뒤쪽의 `j = 11 (A₁→B₁)`, `j = 14 (A₂→B₂)`는
  같은 지지를 갖지만 기여가 무시할 만하다. **결함이 일어나는 중간 회로 기저와 이후 게이트의
  전파**가 결정적이다.

**대칭성 설명 — 양측 Pauli 공변성(bilateral-Pauli covariance).** Bell-대각 연산자는
`GρG = ρ`, `G ∈ {XX, ZZ}`를 만족한다. 만약 이중선형 맵이

```
B_q^(k)(GAG, GBG) = G B_q^(k)(A,B) G ,    G ∈ {XX, ZZ}
```

를 만족하면 Bell-대각 입력이 Bell-대각으로 남는다(정규화는 대칭성을 바꾸지 않으므로 **충분조건**).
16×16 Pauli 기저 쌍 **전부**에 대해 `q = 0, 0.001, 0.01, 0.03`에서 검사한 최대 잔차:

| 회로 | G = XX | G = ZZ |
|--|--|--|
| Step 3 | 1.78e-15 | 0 |
| Step 4 | 6.30e-15 | 1.86e-15 |
| Step 5 (보정) | **2.4319e-2** | **2.8087e-2** |

보정된 Step 5도 `q = 0`에서는 잔차가 `O(1e-15)` — **무노이즈 목표 연산 자체는 대칭성과
양립하며, CNOT 노이즈가 Step 5 분해에 끼어들 때 비로소 깨진다.**

**회로 수준 해석.** 이상 분해 `U = G_m G_{m−1} ⋯ G₁`에서는 총곱만이 중요하다. 그러나 국소
노이즈가 중간에 삽입되면 결함의 응답은 (i) 그 위치의 상태/연산자 기저, (ii) 국소 결함의 지지,
(iii) 이후에 작용하는 게이트에 의존한다 — 이것이 정확히 `L_j = U_{>j} ∘ (R_j − I) ∘ U_{≤j}`이다.
따라서 무노이즈에서 같은 `U_PQEC`로 맞춰져 있어도 **끼워진 노이즈 맵은 같을 필요가 없다.**
Step 5에서는 여러 중간 CNOT 결함이 `XY − YX`, `XZ + ZX`, `YZ − ZY` 조합으로 전파된다.

> **주의 (노트가 명시한 범위)**: 이는 `RX, RY, RZ` 회전 자체가 Bell-대각 대칭성을 깬다는 것을
> 입증하지 않는다. 지지되는 결론은 더 좁다 — *이 특정 학습 분해에서 회전들이 노이즈 CNOT 위치에
> 대해 놓인 배치*가 국소 결함을 off-Bell 방향으로 전파시킨다는 것.

---

## 4. 핵심 결론

### 4.1 한 라운드 순위 (축 A)

`ε₂*` 임계값은 **CNOT 개수가 아니라 배치**가 정한다.

- Step 3 (16) → Step 4 (14): **1.25–1.42×** — 개수비 `16/14 ≈ 1.14`보다 크다
- Step 4 (14) → Step 5 (14): **1.5–1.9×** — **개수가 같은데도** 차이
- 파괴적 2-CNOT: **2.7–4.4×** — 관측량 완화가 절감의 유일한 출처

### 4.2 반복 동역학 순위 (축 B) — 한 라운드 순위와 어긋난다

| | Step 3 | Step 4 | Step 5 |
|--|--|--|--|
| CNOT 개수 | 16 | 14 | 14 |
| 한 라운드 1차 손실 | 17/8 q | 7/4 q | **5/4 q** (최소) |
| Bell-대각 섹터 보존 | **예** | **예** | **아니오** |
| off-Bell 생성 | 없음 | 없음 | **`O(q)`에서 생성** |
| 고충실도 영역 | 고정 상태 | 더 높은 고정 상태 | **준안정 대지** |
| 장시간 거동 | Bell 섹터 고정 상태 | Bell 섹터 고정 상태 | **분리 가능 곱 상태로 탈출** |
| 고정점 존재 한계 | `q_SN ≈ 0.13058` | `q_SN ≈ 0.18942` | (고정 원, 각 방향 marginal) |
| 얽힘 한계 | `q_ent ≈ 0.12914` | `q_ent ≈ 0.18474` | `C = 0` |

`ε̄ = 0.9, q = 0.01` 공통 조건에서:
Step 3 → 0.9783 (n≥10부터 5000까지 평평), Step 4 → 0.9824 (동일하게 평평),
Step 5 → 0.9875에서 ~500 라운드 유지 후 `F₁₀₀₀ ≈ 0.544`, `F₅₀₀₀ ≈ 0.408`.

**⟹ 한 라운드 Bell 충실도가 가장 좋은 회로가 장시간 반복 거동에서도 가장 좋은 것은 아니다.**

### 4.3 왜 다른가 — 통합된 인과 사슬

```
학습된 중간 회로 기저에서 국소 CNOT 결함이 발생
   ↓  (L_j = U_{>j} ∘ (R_j − I) ∘ U_{≤j} — 이후 게이트가 결함을 전파)
양측 Pauli 공변성 B(GAG,GBG) = G B(A,B) G 가 깨짐  (G ∈ {XX, ZZ})
   ↓
Bell-대각 섹터가 더 이상 불변이 아님 — O(q)에서 XY−YX, XZ+ZX, YZ−ZY 생성
   ↓
Step 3/4에서도 이미 존재하던 (ZI+IZ)/√2 방향의 전체 상태 불안정성에 씨앗 공급
   ↓
준안정 대지에서 탈출 → 순도는 높지만 얽힘이 0인 분리 가능 곱 상태
```

Step 3/4는 정확한 산술에서 이 씨앗을 **스스로 만들지 않기 때문에** Bell-대각 불변 다양체 위에
머문다. 그 고정점들 역시 전체 공간에서는 안장점이지만, 노이즈 모형이 불안정 방향을 여기시키지
않는다.

**연구 함의**: *CNOT 개수, 회로 깊이, 무노이즈 유니터리 충실도만으로는 반복 적용되는 노이즈 PQEC
회로의 성능을 특징지을 수 없다.* 회로의 **노이즈 유도 대칭성**이 함께 고려되어야 하며, 이는
PQEC 구현을 비교하거나 설계할 때의 새로운 설계 기준이 된다.

---

## 5. 저장소 상태

### 5.1 브랜치 맵

| 브랜치 | 내용 | 헤드라인 결과 |
|--|--|--|
| `main` | 통합 안정 브랜치 (모든 단계, 비교 코드, 전체 write-up, 논문 그림) | — |
| `setup-bell-input` | Setup — Bell-등방성 입력 + 검증 | `max\|ρ−ρ_t\| ≈ 2.8e-16`; `ε < 2/3` 에서 얽힘 |
| `step-1-ideal-pqec` | Step 1 — 이상 1라운드 기준선 | `F₁ = (1+3t)²/(4(1+3t²))`, `~1e-16` 검증 |
| `step-2-fredkin-noise` | Step 2 — CSWAP 전역 감극 | 자기-완화: **임계값 없음**, `N ∼ (1−g)⁻⁴` |
| `step-3-textbook-16cnot` | Step 3 — 교과서 16-CNOT | `ε₂*` = 0.033 → 0.126 (ε = 0.1 → 0.6) |
| `step-4-resynthesized-14cnot` | Step 4 — 재합성 14-CNOT | `ε₂*` = 0.041 → 0.178 (**1.25–1.42×**) |
| `step-5-learned-14cnot` | Step 5 — 학습 14-CNOT | `ε₂*` = 0.060 → 0.338 (**1.5–1.9×**) |
| `iterated-noisy-pqec` | 반복 라운드 연구 — 유효 비선형 맵의 고정점 | `1−F_*(q) = A q`, `A` = 2.13/1.75/1.25; 고정점은 **안장점** |
| `claude/theoretical-update-review-39lkhv` | 위 + `fidelity_vs_round` 그림/데이터 (노트 00 Figure 1) | 3회로 공통 조건 5000 라운드 |
| `archive-branched-substeps` | 단계 재번호화 이전 스냅샷 | 구 3a/3b/4a/4b/5a/5b/5c 라벨 보존 |

각 단계 브랜치는 프로젝트 **전체 사본**이라 독립적으로 체크아웃·실행 가능하며,
브랜치별 `README.md` + `STEP_GUIDE.md`를 가진다.

### 5.2 반복 동역학 코드의 검증 게이트

`iterated-noisy-pqec` 브랜치는 어떤 스윕보다 먼저 다음을 통과시킨다:

| 검사 | 결과 |
|--|--|
| 한 라운드 `Q`, `N_Φ`, `F` vs 저장소 PennyLane 실행기 | ≤ 1.5e-15 (세 회로 전부) |
| vs Step 3 / Step 4 닫힌 형태 | ≤ 1.4e-15 |
| 치환 채널 정의 vs `global_depol_kraus` | 9.3e-17 |
| `q = 0`에서 `τ_A = ρ²` | 9.5e-16 (S3), 5.5e-16 (S4), **6.9e-9 (S5)** |

Step 5의 바닥값은 저장된 학습 파라미터에 내재된 것(`δ_U ≈ 3e-15` 충실도 = `2e-7` 진폭)이며
**패치하지 않고 문서화**했다 — 노트 03/04의 보정 모형이 정확히 이 잔차를 다룬다.

**물리성 검사**: 모든 반복이 PSD (`min_eig ≥ −1e-16`), 단위 대각합, `Q ≥ 0.236` (분모 붕괴 없음).
**발견된 수치 인공물**: 반-에르미트 부동소수 잔여가 라운드마다 **정확히 ×2** 증폭되어
(`ρ⊗ρ`에 슬롯이 둘이므로) `n ≈ 53`에서 `O(1)`에 도달 — 매 반복을 에르미트 다양체로 사영하고
버려진 노름을 기록(`~1e-16` 유지)하여 해결.

**초기 상태 독립성**: `ε₀ ∈ {0.05 … 0.5}` 전부가 같은 상태에 도달 (퍼짐 ≤ 3.3e-13), 양방향에서 접근.

### 5.3 문서–코드 정합 상태 (갭)

- `main`의 `README.md` / `PROGRESS.md`는 **2026-07-30까지**만 반영한다. 8월의 반복 동역학
  결과(노트 01–04의 닫힌 형태, saddle-node/얽힘 한계, 전체 상태 안장점, Step 5 탈출과 곱 상태
  고정 원, 공변성 검사)는 브랜치에만 있고 `main` 문서에는 브랜치 맵 한 줄로만 등장한다.
- `iterated-noisy-pqec` 브랜치 README는 "Step 5가 셋 중 최선"이라는 초기 표현과
  "~200–300 라운드 후 이탈"을 담고 있는데, 노트 00/03의 **보정된 모형** 기준
  (`ε̄ = 0.9, q = 0.01`)으로는 대지가 ~500 라운드까지 유지되고 `n ≈ 1000`에서 붕괴한다.
  두 수치는 조건·보정 여부가 달라 모순은 아니지만, 최종 서술은 노트 00의
  "준안정 대지 → 탈출 → 분리 가능 곱 상태" 프레이밍으로 통일하는 편이 안전하다.

---

## 6. 이 정리를 작성하며 수행한 독립 확인

노트 01/02의 Step 3·Step 4 고정점 닫힌 형태를 독립적으로 수치 재구현하여 대조했다.

| 확인 항목 | 노트 값 | 재계산 값 |
|--|--|--|
| `F_*^(3)` (q = 0.01) | 0.978346 | 0.978346 |
| `F_*^(4)` (q = 0.01) | 0.982351 | 0.982351 |
| `F_*^(3)` (q = 0.05) | 0.881237 | 0.881237 |
| `F_*^(4)` (q = 0.05) | 0.908138 | 0.908138 |
| `q_SN^(3)` | 0.130579267969 | 0.13057926796879 |
| `q_SN^(4)` | 0.189416655885 | 0.18941665588510 |
| `q_ent^(3)` | 0.129137470776 | 0.12913747077600 |
| `q_ent^(4)` | 0.184735805365 | 0.18473580536464 |
| `(1−F_*^(3))/q → 17/8` | 2.125 | 2.1250039 (q = 1e-6) |
| `(1−F_*^(4))/q → 7/4` | 1.75 | 1.7500014 (q = 1e-6) |

`u_*, v_*` 대표값 표도 전부 일치한다. 노트의 해석적 결과는 자기 정합적이다.

---

## 7. 남은 과제 / 다음 단계 후보

**축 A에서 이월된 항목**

- **최적화된 분해**: Cruz–Murta 7-CNOT (arXiv:2305.18128), Smolin–DiVincenzo 5개
  2-큐빗 게이트 (PRA 53, 2855 (1996)) 를 같은 CNOT-only 모형에서 8-CNOT 일반 분해와 비교
- 얽힘(concurrence/negativity) 임계값과 다중 라운드 누적
- 결맞은(coherent) / 편향된 CNOT 오류 모형

**축 B에서 노트가 명시한 열린 문제**

- Step 5의 곱 상태 고정 원은 **수치적으로 인증된 반해석적 결과** — 학습된 회전 파라미터를
  모두 소거한 **완전한 기호적 유도**는 아직 없음
  (테스트된 `q ∈ {0.003, 0.01, 0.05, 0.1}` 에서 `‖M(ρ_*) − ρ_*‖_F ∼ 1e-11–1e-9`)
- 고정 원은 테스트된 Bell-등방성 초기 조건에서 관찰된 점근족일 뿐 — **전체 고정점 집합의
  완전한 분류는 아님**
- Step 3/4 공변성 검사는 수치적으로 기저-완전하지만 **모든 `q`에 대한 기호적 증명은 아님**
  (`q = 0, 0.001, 0.01, 0.03` 에서 명시적으로 검사됨)
- `t_j^(3,4) ≈ 0` 은 **순수 Bell 입력에 대한 1차 응답**의 진술 — 임의 입력 연산자에 대한
  개별 단일 결함 초연산자의 **전역 공변성 증명은 아님**
- Step 3/4의 씨앗된 분리 가능 종점은 `q = 0.01, 0.05` 에서만 검사됨 — 전체 노이즈 매개변수
  구간에서의 모든 전체 상태 고정점과 흡인 영역 분류는 미완
- `RX, RY, RZ` 회전 **자체**가 대칭성을 깨는지는 미해결 — 지지되는 진술은 "이 특정 학습
  분해에서의 배치" 로 한정됨

**문서화 작업**

- `main`의 `README.md` / `PROGRESS.md`에 8월 반복 동역학 결과 반영
- `iterated-noisy-pqec` README의 Step 5 서술을 노트 00/03 프레이밍으로 통일
- 노트 5편을 저장소 내 write-up (`ITERATED_DYNAMICS.md` 등)으로 이관 검토

---

## 8. 참고 — 저장소 문서 지도

| 문서 | 내용 |
|--|--|
| `README.md` | 프로젝트 전체 개요, Setup–Step 5, 보조 비교, 파일 표, 실행법 |
| `PROGRESS.md` | 날짜별 진행 로그 (2026-07-21 ~ 2026-07-30) |
| `CNOT_NOISE_ANALYSIS.md` | Step 3 전체 노트: 표기/변수 정의, 이론(Part I), 구현·검증(Part II) |
| `SWAP_GADGET_OPTIMIZATION.md` | 두 CNOT 절감 경로 (동일-유니터리 16→14; 동일-관측량 2 CNOT) |
| `PQC_APPROX.md` | Step 5 write-up: 게이지-정합 ansatz, 14로 가지치기, 임계값, isometry/관측량 완화 |
| `PQC_CIRCUITS_FOR_ANALYSIS.md` | 학습 회로들의 정확한 게이트 목록 + 블록별 병합 SU(2) 행렬 (독립 해석 검증용) |
| `figures/` | 논문용 그림 (Setup, Step 1–5 회로, 임계값 비교) |
| `results/iterated_pqec/`, `figures/iterated_pqec/` | 반복 동역학 스윕 결과·그림 (브랜치) |
