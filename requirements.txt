# Python dependencies for the PQEC operational-threshold project.
#
# Install with:  pip install -r requirements.txt
#
# Minimum versions are what the code requires; the exact versions the code was
# developed and verified against are noted in the trailing comments.

pennylane>=0.45      # verified with 0.45.1  (mixed-state simulator, QubitChannel)
numpy>=2.0           # verified with 2.4.6
scipy>=1.10          # verified with 1.17.1  (L-BFGS-B for Step-5 PQC training)
matplotlib>=3.8      # verified with 3.11.1  (circuit drawing + Step-5 figures)

# Tested on Python 3.11.

# Optional (only for resynthesize_gadget.py / draw_resynth.py, Step 4a):
#   qiskit>=1.0      # verified with 2.5.1
#   pytket           # verified with 2.18.1
#   pylatexenc       # only for draw_resynth.py (qiskit matplotlib circuit drawer)
